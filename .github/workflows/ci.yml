name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  gitleaks_scan:
    name: Gitleaks Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  lint_build:
    name: Lint + Build
    runs-on: ubuntu-latest
    needs: gitleaks_scan
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint -- --max-warnings=0

      - name: Build
        run: |
          if [ -z "${NEXT_PUBLIC_SUPABASE_URL:-}" ] || [ -z "${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}" ]; then
            echo "Missing Supabase public env -> skip build"
            exit 0
          fi
          if [ -z "${SUPABASE_SERVICE_ROLE_KEY:-}" ]; then
            echo "Missing Supabase service role -> skip build"
            exit 0
          fi
          npm run build

      - name: Verify debug plan route disabled in production
        run: npm run check:debug-plan-prod

      - name: Upload lint/build artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: lint-build-failure-artifacts
          path: |
            .next/**
            test-results/**
            playwright-report/**
          if-no-files-found: ignore

  stable_tests:
    name: Stable Tests
    runs-on: ubuntu-latest
    if: always()
    needs: lint_build
    env:
      TEST_BASE_URL: http://127.0.0.1:3100
      API_KEY: ${{ secrets.API_KEY }}
      TRIAL_EMAIL: ${{ secrets.TRIAL_EMAIL }}
      TRIAL_PASSWORD: ${{ secrets.TRIAL_PASSWORD }}
      PREMIUM_EMAIL: ${{ secrets.PREMIUM_EMAIL }}
      PREMIUM_PASSWORD: ${{ secrets.PREMIUM_PASSWORD }}
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Skip stable tests if Supabase secrets missing
        run: |
          if [ -z "${NEXT_PUBLIC_SUPABASE_URL:-}" ] || \
             [ -z "${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}" ] || \
             [ -z "${SUPABASE_SERVICE_ROLE_KEY:-}" ]; then
            echo "SKIP_SUPABASE_CHAIN=true" >> "$GITHUB_ENV"
            echo "Skipping Stable Tests: Supabase secrets missing"
            exit 0
          fi
          echo "SKIP_SUPABASE_CHAIN=false" >> "$GITHUB_ENV"

      - name: Checkout
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        uses: actions/checkout@v4

      - name: Setup Node 20
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        run: npm ci

      - name: Validate required secrets
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        run: |
          required_vars=(
            API_KEY
          )
          for var_name in "${required_vars[@]}"; do
            if [ -z "${!var_name:-}" ]; then
              echo "Missing required secret/env: $var_name" >&2
              exit 1
            fi
          done

      - name: Lint
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        run: npm run lint -- --max-warnings=0

      - name: Build
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        run: npm run build

      - name: Check trial/premium login env
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        run: |
          if [ -z "${TRIAL_EMAIL:-}" ] || \
             [ -z "${TRIAL_PASSWORD:-}" ] || \
             [ -z "${PREMIUM_EMAIL:-}" ] || \
             [ -z "${PREMIUM_PASSWORD:-}" ]; then
            echo "SKIP_STABLE_SUITE=true" >> "$GITHUB_ENV"
            echo "Missing trial/premium login env -> skip stable suite"
            exit 0
          fi
          echo "SKIP_STABLE_SUITE=false" >> "$GITHUB_ENV"

      - name: Start app
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' && env.SKIP_STABLE_SUITE != 'true' }}
        run: |
          npm run start -- -p 3100 --hostname 127.0.0.1 > /tmp/next-start.log 2>&1 &
          echo "APP_PID=$!" >> "$GITHUB_ENV"
          for i in {1..90}; do
            if curl -fsS http://127.0.0.1:3100/login > /dev/null; then
              exit 0
            fi
            sleep 2
          done
          cat /tmp/next-start.log
          exit 1

      - name: Wait for panel-health readiness
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' && env.SKIP_STABLE_SUITE != 'true' }}
        shell: pwsh
        run: |
          $maxAttempts = 30
          $sleepSeconds = 2
          $healthUrl = "$env:TEST_BASE_URL/api/panel-health"

          for ($attempt = 1; $attempt -le $maxAttempts; $attempt++) {
            try {
              Invoke-WebRequest -Uri $healthUrl -UseBasicParsing | Out-Null
              Write-Host "panel-health is ready: $healthUrl (attempt $attempt/$maxAttempts)"
              exit 0
            } catch {
              Write-Host "panel-health not ready yet (attempt $attempt/$maxAttempts)"
              Start-Sleep -Seconds $sleepSeconds
            }
          }

          Write-Host "panel-health did not become ready in time: $healthUrl"
          if (Test-Path "/tmp/next-start.log") {
            Get-Content "/tmp/next-start.log"
          }
          exit 1

      - name: Run stable suite
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' && env.SKIP_STABLE_SUITE != 'true' }}
        run: npm run test:stable

      - name: Stop app
        if: ${{ always() && env.SKIP_SUPABASE_CHAIN != 'true' && env.SKIP_STABLE_SUITE != 'true' }}
        run: |
          if [ -n "${APP_PID:-}" ]; then
            kill -9 "$APP_PID" || true
          fi

      - name: Upload stable suite reports
        if: ${{ always() && env.SKIP_SUPABASE_CHAIN != 'true' && env.SKIP_STABLE_SUITE != 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: stable-suite-reports
          path: |
            testsprite_tests/generated/*.json
            testsprite_tests/generated/*.cjs
          if-no-files-found: ignore

      - name: Upload stable test diagnostics on failure
        if: ${{ failure() && env.SKIP_SUPABASE_CHAIN != 'true' && env.SKIP_STABLE_SUITE != 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: stable-tests-failure-diagnostics
          path: |
            /tmp/next-start.log
            test-results/**
            playwright-report/**
          if-no-files-found: ignore

  rls_negative_test:
    name: RLS Negative Test
    runs-on: ubuntu-latest
    needs: [gitleaks_scan, lint_build]
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Skip RLS negative test if Supabase secrets missing
        run: |
          if [ -z "${NEXT_PUBLIC_SUPABASE_URL:-}" ] || \
             [ -z "${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}" ] || \
             [ -z "${SUPABASE_SERVICE_ROLE_KEY:-}" ]; then
            echo "SKIP_SUPABASE_CHAIN=true" >> "$GITHUB_ENV"
            echo "Skipping RLS negative test: Supabase secrets missing"
            exit 0
          fi
          echo "SKIP_SUPABASE_CHAIN=false" >> "$GITHUB_ENV"

      - name: Checkout
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        uses: actions/checkout@v4

      - name: Setup Node 20
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        run: npm ci

      - name: Run RLS negative spec
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        run: npm run test:rls:negative

      - name: Upload RLS artifacts on failure
        if: ${{ failure() && env.SKIP_SUPABASE_CHAIN != 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: rls-negative-failure-artifacts
          path: |
            test-results/**
            playwright-report/**
          if-no-files-found: ignore

  rate_limit_abuse_test:
    name: Rate Limit Abuse Test
    runs-on: ubuntu-latest
    needs: lint_build
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Skip rate-limit tests if Supabase secrets missing
        run: |
          if [ -z "${NEXT_PUBLIC_SUPABASE_URL:-}" ] || \
             [ -z "${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}" ] || \
             [ -z "${SUPABASE_SERVICE_ROLE_KEY:-}" ]; then
            echo "SKIP_SUPABASE_CHAIN=true" >> "$GITHUB_ENV"
            echo "Skipping rate-limit tests: Supabase secrets missing"
            exit 0
          fi
          echo "SKIP_SUPABASE_CHAIN=false" >> "$GITHUB_ENV"

      - name: Checkout
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        uses: actions/checkout@v4

      - name: Setup Node 20
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Check Supabase secrets
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        run: |
          if [ -z "${NEXT_PUBLIC_SUPABASE_URL:-}" ] || [ -z "${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}" ] || [ -z "${SUPABASE_SERVICE_ROLE_KEY:-}" ]; then
            echo "SKIP_SUPABASE_CHAIN=true" >> "$GITHUB_ENV"
            echo "Supabase secrets missing in CI -> skipping Rate Limit Abuse Test steps."
            exit 0
          fi
          echo "SKIP_SUPABASE_CHAIN=false" >> "$GITHUB_ENV"

      - name: Install dependencies
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        run: npm ci

      - name: Run rate-limit abuse spec
        if: ${{ env.SKIP_SUPABASE_CHAIN != 'true' }}
        run: npm run test:abuse:rate-limit

      - name: Upload rate-limit artifacts on failure
        if: ${{ failure() && env.SKIP_SUPABASE_CHAIN != 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: rate-limit-failure-artifacts
          path: |
            test-results/**
            playwright-report/**
          if-no-files-found: ignore
